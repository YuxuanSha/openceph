{
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "check_endpoint",
        "description": "Check an endpoint's health status and latency",
        "parameters": {
          "type": "object",
          "properties": {
            "url": { "type": "string", "description": "Endpoint URL to check" },
            "timeout": { "type": "integer", "description": "Timeout in seconds", "default": 10 }
          },
          "required": ["url"]
        }
      }
    }
  ]
}
