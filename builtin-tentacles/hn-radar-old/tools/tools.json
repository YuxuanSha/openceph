{
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "search_hn",
        "description": "Search Hacker News via Algolia API for posts matching a topic",
        "parameters": {
          "type": "object",
          "properties": {
            "query": { "type": "string", "description": "Search query term" },
            "limit": { "type": "integer", "description": "Max results to return", "default": 20 }
          },
          "required": ["query"]
        }
      }
    }
  ]
}
